<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jralison.anestweb">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/ActivitySplash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login"
            android:screenOrientation="portrait"
            android:theme="@style/ActivitySplash" />
        <activity
            android:name=".SplashCadastroActivity"
            android:theme="@style/ActivitySplash" />
        <activity
            android:name=".CadastroProfissionalActivity"
            android:label="@string/title_activity_cadprofissional" />
        <activity
            android:name=".ListagemPacientesActivity"
            android:label="@string/title_activity_listagem_pacientes" />
        <activity
            android:name=".CadastroPacienteActivity"
            android:label="@string/title_activity_cadastro_paciente"
            android:parentActivityName=".ListagemPacientesActivity" />
    </application>

</manifest>